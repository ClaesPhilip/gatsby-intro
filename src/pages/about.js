import React from 'react';
import { Link } from 'gatsby';
import Layout from '../components/layout';

export default () => (
    <Layout>
    <h1>OM OSS</h1>
    <p>This is my first Gatsby site</p>
    <Link to='/'>&larr; tillbaka till landingssidan</Link>
    </Layout>
);
